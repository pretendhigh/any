# Codex Pet Generator Prompt

把下面这段 prompt 粘贴给 Codex，并附上一张人物/角色形象参考图。默认会按本项目的 Any 风格生成一个 Codex pet；你也可以在 prompt 末尾改名字、风格、动作和特殊状态。

```text
请根据我附上的人物形象，制作一个 Codex pet。

默认要求：
- 输出一个可安装的 Codex custom pet 包，包含 `pet.json` 和 `spritesheet.webp`。
- 最终 atlas 必须符合 Codex pet 规格：`1536x1872`、8 列 x 9 行、每格 `192x208`、RGBA/WebP、未使用格透明。
- 角色默认使用可爱动漫 chibi / pixel-art-adjacent Codex pet 风格：厚实深色轮廓、扁平赛璐璐阴影、紧凑轮廓、192x208 下清晰可读。
- 保留参考图的核心身份特征：发型、发色、耳朵/尾巴/配饰、服装风格、表情气质、主色调。
- 不要把静态图硬拆成木偶再手动画腿或手。每个动作行必须生成完整姿态序列，身体、膝盖、手臂、头发、耳朵、尾巴要连贯自然。
- 不要有文字、编号、网格线、背景场景、投影、速度线、漂浮符号或断开的效果碎片。

默认动作行：
1. `idle`：打瞌睡/困倦点头，闭眼或半闭眼，轻微身体摇晃。
2. `running-right`：向右走/跑，8 帧，膝盖和脚步自然交替，手臂与尾巴配合摆动。
3. `running-left`：向左走/跑，8 帧，可在确认无文字、无方向性道具时由右跑镜像生成。
4. `waving`：可爱挥手，4 帧，只用手部姿态表达，不要画波浪线。
5. `jumping`：可爱跳跃，5 帧，包含蹲下、起跳、最高点、下落、落地回弹。
6. `failed`：失败状态，8 帧，默认是角色把自己装进小垃圾箱，害羞/沮丧地探头、下沉、耳朵耷拉。
7. `waiting`：期待等待，6 帧，大眼睛眨眼、双手握在胸前、轻微弹跳。
8. `running`：任务进行中，6 帧，疯狂敲代码/敲键盘，不是字面跑步；屏幕可有不可读的绿色线条。
9. `review`：拿着小放大镜检查/查找，6 帧，放大镜必须握在手里，不能漂浮。

制作流程：
1. 先根据参考图生成一个干净的 base sprite，作为所有动作的身份基准。
2. 再逐行动作生成完整横向 sprite strip。每行都用 base sprite 和原参考图约束身份一致性。
3. 对每行做视觉检查，尤其检查：身体比例、四肢连接、膝盖动作、道具是否握在手里、是否有漂浮碎片。
4. 抽帧到 `192x208` 单元格，组合成 8x9 atlas。
5. 输出 QA：`qa/contact-sheet.png`、每个状态的 GIF 预览、`qa/validation.json`。
6. 安装到 `${CODEX_HOME:-$HOME/.codex}/pets/<pet-id>/`，或者至少把可安装包保存在当前项目的 `<pet-id>/` 目录。

默认命名：
- 如果我没有指定名字，请根据角色取一个短的英文 id。
- `pet.json` 中使用：
  - `id`: 小写短横线 slug
  - `displayName`: 可读名称
  - `description`: 一句英文简介
  - `spritesheetPath`: `spritesheet.webp`
  - `kind`: `person`（除非角色明显不是人形）

如果我额外指定了动作、服装、画风或 failed 状态，请优先使用我的指定，但仍保持 Codex pet atlas 规格和动作连贯性。
```

## 可选自定义项

在 prompt 末尾追加类似内容即可：

```text
自定义：
- pet 名称：Mika
- 服装：黑色女仆装
- failed：钻进纸箱，不使用垃圾箱
- running：调试电路板，不敲代码
- review：拿望远镜，不拿放大镜
- 风格：更偏像素风，减少高光和细节
```
